export interface Mall {
  id: number;
  name: string;
  lat: number;
  lon: number;
  // station: string;
  // colors: string;
  // services: number;
  // halals: number;
  // stores: number;
  // floors: number;
  // retail_area: number;
  // opening_year: number;
}